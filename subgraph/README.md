# Orbitt Subgraph

Hosted service slug: `orbitt-market` (0gTestnet).

## Install
```bash
cd subgraph
npm install
```

## Generate types
```bash
npm run codegen
```

## Build
```bash
npm run build
```

## Deploy (GAP)
The Graph hosted service CLI credentials and deploy command are not specified in the blueprint.
