# openclaw-0rbit placeholder

This directory is held for the [OpenClaw](#) framework fork described in Section 4 of the blueprint. Actual fork contents will replace this placeholder once the upstream repository URL is confirmed.

## Status
- **Fork target:** `openclaw-0rbit` (per blueprint Section 14 Phase 0 Item 1)
- **Blocking [GAP]:** The blueprint does not include the original OpenClaw GitHub URL, so cloning/forking cannot proceed yet.

## Next steps
1. Obtain the exact OpenClaw repository URL referenced in the blueprint so it can be cloned into this directory.
2. Replace this placeholder with the cloned `openclaw-0rbit` fork contents and document any divergence.
3. Defer the plugin-integration work until Milestone M4, as specified in the plan.
